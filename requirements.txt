flask==3.0.0
flask-cors==4.0.0
nltk==3.8.1
# AI / NLP stack (versions aligned for compatibility)
huggingface-hub==0.25.2
transformers==4.40.2
sentence-transformers==2.7.0
# Heavy ML dependencies: relaxed ranges to allow compatible wheels on common hosts
# If you don't need embeddings/LLMs on the server, consider moving these to
# a separate `requirements-ml.txt` and only install them when required.
# Pick a torch version available on the PyPI index used by Render.
torch>=2.5.0,<2.9.0
# torchvision should be compatible with the selected torch version; keep a
# permissive range rather than pinning a single patch release.
torchvision>=0.17.2,<0.19.0
accelerate>=0.30.1

# (Optional) numpy explicitly if needed
numpy>=1.24.0
